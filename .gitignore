# Dependencies
node_modules/

# Build output
dist/
dist-ssr/
*.local

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
*.swp

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Excel lock files
~$*.xlsx

# Claude Code 로컬 설정
.claude/settings.local.json

# 이미지 생성 API 키. 저장소가 Public이므로 절대 커밋하지 않는다.
.env
.env.local

# 제출 문서. 저장소가 Public이라 대회 마감 전에 공개하지 않는다.
# 팀원 롤 기술서에는 실명이 들어가므로 특히 커밋하면 안 된다.
# 심사에는 PDF로 직접 제출한다. 근거 자료(docs/)는 그대로 공개한다.
submission/
